-- PASS 1E.1 controlled operational helper only.
-- Installed solely to invoke the already-controlled temporary Edge deployer from the database execution plane.
create extension if not exists http with schema extensions;
