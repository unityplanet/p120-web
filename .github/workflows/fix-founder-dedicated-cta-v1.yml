name: Fix Founder Dedicated CTA v1 (Historical)

# Temporary PASS 4 corrective workflow. CTA destinations are now part of the
# controlled dedicated-page baseline and are regression-tested by PASS 6.
# Retained as a manual historical utility only; no automatic pushes.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  historical-notice:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify current CTA syntax
        run: |
          set -euo pipefail
          node --check founder-editorial-story-v1.0.js
          python qa/check_founder_separate_page.py
          echo 'Historical CTA correction workflow. Current release gate: FOUNDER-WEB PASS 6.'
